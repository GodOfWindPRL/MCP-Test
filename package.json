{
  "name": "mcp-test",
  "version": "0.1.0",
  "description": "MCP Test — TRX price + send funds (privateKey in input or TronLink MCP via agent)",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "mcp-test": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "tronweb": "^6.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  },
  "license": "MIT"
}
